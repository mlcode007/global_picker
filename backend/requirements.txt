# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.32.1

# 数据库
sqlalchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0

# 配置管理
pydantic-settings==2.6.1

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Redis
redis==5.2.1

# 报表导出
openpyxl==3.1.5

# HTTP 客户端（抓取用）
httpx==0.28.1

# 拼多多拍照购：截图裁剪 + 阿里云 OSS
Pillow>=11.0.0
oss2>=2.19.0

# 浏览器自动化（JS 渲染页面采集）
playwright>=1.49.0
playwright-stealth>=2.0.0

# 图像处理（验证码识别等）
numpy>=1.26.0
opencv-python>=4.8.0

# 短信验证（阿里云 SMS 通过 httpx 直接调用 REST API，无需额外 SDK）
