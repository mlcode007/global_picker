"""
支付宝支付流程详解 - 学习指南

本指南详细解释支付宝PC扫码支付的完整流程
"""

# ==================== 支付流程图 ====================
"""
用户端                    你的系统                    支付宝
  |                         |                          |
  |  1. 点击支付            |                          |
  |------------------------>|                          |
  |                         |  2. 创建订单             |
  |                         |  (生成订单号)            |
  |                         |                          |
  |                         |  3. 调用支付宝API        |
  |                         |------------------------->|
  |                         |  创建预下单              |
  |                         |                          |
  |                         |  4. 返回二维码URL        |
  |                         |<-------------------------|
  |                         |                          |
  |  5. 返回二维码          |                          |
  |<------------------------|                          |
  |                         |                          |
  |  6. 扫码支付            |                          |
  |-------------------------------------------------->|
  |                         |                          |
  |                         |  7. 异步通知支付结果     |
  |                         |<-------------------------|
  |                         |  (POST到notify_url)      |
  |                         |                          |
  |                         |  8. 验证签名             |
  |                         |  更新订单状态            |
  |                         |  返回"success"           |
  |                         |------------------------->|
  |                         |                          |
  |  9. 前端轮询查询        |                          |
  |------------------------>|                          |
  |                         |  10. 查询订单状态        |
  |                         |------------------------->|
  |                         |  11. 返回支付结果        |
  |                         |<-------------------------|
  |  12. 显示支付成功       |                          |
  |<------------------------|                          |
"""


# ==================== 核心API说明 ====================
"""
1. alipay_trade_precreate (当面付-预下单)
   - 用途：生成二维码URL，用户扫码支付
   - 参数：
     * out_trade_no: 商户订单号
     * total_amount: 订单金额
     * subject: 订单标题
     * notify_url: 异步通知地址（可选）
   - 返回：二维码字符串

2. alipay_trade_query (查询订单)
   - 用途：查询订单支付状态
   - 参数：out_trade_no 或 trade_no
   - 返回：订单状态、金额等信息

3. alipay_trade_close (关闭订单)
   - 用途：关闭未支付的订单
   - 参数：out_trade_no

4. alipay_trade_refund (退款)
   - 用途：退款
   - 参数：out_trade_no, refund_amount
"""


# ==================== 代码实现要点 ====================
"""
要点1：订单号生成
- 必须唯一
- 建议使用：时间戳 + 随机字符串
- 示例：f"{datetime.now().strftime('%Y%m%d%H%M%S')}{uuid.uuid4().hex[:8]}"

要点2：签名验证（最重要！）
- 收到回调后，第一件事就是验证签名
- 使用支付宝公钥验证
- 验证失败直接拒绝，不处理业务逻辑

要点3：幂等性处理
- 支付宝可能多次发送同一个通知
- 需要检查订单是否已处理过
- 示例：
  if order.status == 'paid':
      return "success"  # 已处理，直接返回成功

要点4：金额校验
- 回调中的金额必须与订单金额一致
- 防止金额篡改攻击
- 示例：
  if float(callback_amount) != float(order.amount):
      raise Exception("金额不匹配")

要点5：异步通知处理
- 必须返回纯文本 "success"
- 不能返回HTML或其他内容
- 处理失败时返回 "fail"，支付宝会重试
"""


# ==================== 常见错误 ====================
"""
1. 签名验证失败
   - 原因：公钥/私钥不匹配
   - 解决：检查密钥是否正确，是否混淆了应用公钥和支付宝公钥

2. 订单号重复
   - 原因：订单号生成逻辑有问题
   - 解决：使用更可靠的唯一ID生成方式

3. 收不到异步通知
   - 原因：notify_url不可访问或返回格式错误
   - 解决：确保URL可公网访问，返回"success"

4. 沙箱环境测试问题
   - 需要用沙箱版支付宝APP
   - 沙箱账号余额不足（可在沙箱控制台充值）
"""


# ==================== 测试步骤 ====================
"""
1. 准备阶段
   - 注册支付宝开放平台账号
   - 创建应用，获取APP_ID
   - 生成RSA2密钥对
   - 配置应用公钥，获取支付宝公钥

2. 沙箱测试
   - 使用沙箱环境配置
   - 下载沙箱版支付宝APP
   - 用沙箱账号登录
   - 创建测试订单，扫码支付

3. 验证回调
   - 使用ngrok等工具将本地服务暴露到公网
   - 或在服务器上部署测试
   - 检查是否能收到异步通知

4. 生产部署
   - 切换到生产环境配置
   - 使用真实的APP_ID和密钥
   - 确保notify_url是HTTPS
"""


if __name__ == "__main__":
    print("支付宝支付流程详解")
    print("请查看文件中的注释了解详细信息")
